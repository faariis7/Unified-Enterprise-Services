import type { RequestApproval } from './request-approval-model';
import type { Workspace } from './workspace-model';

export interface RequestApprovalSchedule {
  /**
   * @displayName Request Approval Schedule ID
   * @description Unique system identifier - GUID generated by system
   * @uiConstraints NEVER create input fields for this ID
   */
  id: string;
  /**
   * @displayName Idempotency Key
   * @description Human-readable record identifier
   * @validationRule Required for create/update operations
   */
  idempotencyKey: string;
  /**
   * @displayName Escalation At
   * @validationRule Required for create/update operations
   */
  escalationAt: string;
  /**
   * @displayName Expires At
   * @validationRule Required for create/update operations
   */
  expiresAt: string;
  /**
   * @displayName Reminder At
   * @validationRule Required for create/update operations
   */
  reminderAt: string;
  /**
   * @displayName Request Approval
   * @validationRule Required for create/update operations
   */
  requestApproval: Pick<RequestApproval, 'id' | 'approvalName'>;
  /**
   * @displayName Workspace
   * @validationRule Required for create/update operations
   */
  workspace: Pick<Workspace, 'id' | 'workspaceName'>;
}

export const _RequestApprovalSchedule = 'RequestApprovalSchedule' as const;