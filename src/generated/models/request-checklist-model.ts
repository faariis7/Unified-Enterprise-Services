import type { Person } from './person-model';
import type { Request } from './request-model';
import type { Workspace } from './workspace-model';

export const RequestChecklistStatusKeyToLabel = {
  'Open': 'Open',
  'Completed': 'Completed',
  'Cancelled': 'Cancelled'
} as const;
export type RequestChecklistStatusKey = keyof typeof RequestChecklistStatusKeyToLabel;

export interface RequestChecklist {
  /**
   * @displayName Request Checklist ID
   * @description Unique system identifier - GUID generated by system
   * @uiConstraints NEVER create input fields for this ID
   */
  id: string;
  /**
   * @displayName ChecklistName
   * @description Human-readable record identifier
   * @validationRule Required for create/update operations
   */
  checklistName: string;
  /**
   * @displayName CreatedAt
   * @validationRule Required for create/update operations
   */
  createdAt: string;
  /**
   * @displayName CreatedBy
   * @validationRule Required for create/update operations
   */
  createdBy: Pick<Person, 'id' | 'displayName'>;
  /**
   * @displayName RequestId
   * @validationRule Required for create/update operations
   */
  requestId: Pick<Request, 'id' | 'requestNumber'>;
  /**
   * @displayName Status
   * @validationRule Required for create/update operations
   */
  statusKey: RequestChecklistStatusKey;
  /**
   * @displayName WorkspaceId
   * @validationRule Required for create/update operations
   */
  workspaceId: Pick<Workspace, 'id' | 'workspaceName'>;
}

export const _RequestChecklist = 'RequestChecklist' as const;